const myObject = {
    js : "javascript",
    cpp: "c++",
    rb : "ruby",
    swift : "swift by apple"
}

for (const key in myObject) {
   console.log(`${key} shortcut is for ${myObject[key]}`);
   }

const programing = ["js", "cpp", "py", "java", "ruby"]
for (const key in programing) {
//    console.log(programing[key]);
   
}


for (const key in map) {
    console.log(key);
    
}